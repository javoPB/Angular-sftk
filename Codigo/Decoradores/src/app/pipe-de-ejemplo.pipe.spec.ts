import { PipeDeEjemploPipe } from './pipe-de-ejemplo.pipe';

describe('PipeDeEjemploPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDeEjemploPipe();
    expect(pipe).toBeTruthy();
  });
});
